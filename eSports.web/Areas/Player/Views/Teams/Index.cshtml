@model IEnumerable<Team>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3 mt-5" style="background-color: #f1f1f1">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">T List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Teams" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp;Create New
            </a>

        </div>
    </div>
    <br/><br/>


    <table class="table table-bordered table-striped" style="width: 100%">
        <thead>
        <tr>
            <th>T Name</th>
            <th>Action</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var team in Model)
        {
            <tr>
                <td width="40%">@team.Name</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Teams" asp-action="Edit" asp-route-id="@team.Id"
                           class="btn btn-info m-2">
                            <i class="bi bi-pencil-square"></i> Edit
                        </a>
                        <a asp-controller="Teams" asp-action="Delete" asp-route-id="@team.Id"
                           class="btn btn-warning m-2">
                            <i class="bi bi-trash-fill"></i> Delete
                        </a>
                    </div>
                </td>

            </tr>
        }
        </tbody>
    </table>
</div>